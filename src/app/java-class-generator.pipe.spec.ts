import { JavaClassGeneratorPipe } from './java-class-generator.pipe';

describe('JavaClassGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new JavaClassGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
